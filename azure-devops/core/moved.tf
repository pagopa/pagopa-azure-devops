moved {
  from = module.DEV-AZURERM-SERVICE-CONN
  to   = module.dev_azurerm_service_conn
}

moved {
  from = module.UAT-AZURERM-SERVICE-CONN
  to   = module.uat_azurerm_service_conn
}

moved {
  from = module.PROD-AZURERM-SERVICE-CONN
  to   = module.prod_azurerm_service_conn
}

moved {
  from = module.DEV-TLS-CERT-SERVICE-CONN
  to   = module.dev_tls_cert_service_conn
}

moved {
  from = module.UAT-TLS-CERT-SERVICE-CONN
  to   = module.uat_tls_cert_service_conn
}

moved {
  from = module.PROD-TLS-CERT-SERVICE-CONN
  to   = module.prod_tls_cert_service_conn
}

# moved {
#   from = module.DEV-ECOMMERCE-TLS-CERT-SERVICE-CONN
#   to   = module.dev_ecommerce_tls_cert_service_conn
# }
#
# moved {
#   from = module.UAT-ECOMMERCE-TLS-CERT-SERVICE-CONN
#   to   = module.uat_ecommerce_tls_cert_service_conn
# }
#
# moved {
#   from = module.PROD-ECOMMERCE-TLS-CERT-SERVICE-CONN
#   to   = module.prod_ecommerce_tls_cert_service_conn
# }

moved {
  from = module.DEV-ECOMMERCE-TLS-CERT-SERVICE-CONN
  to   = module.dev_ecommerce_tls_cert_service_conn
}

moved {
  from = module.UAT-ECOMMERCE-TLS-CERT-SERVICE-CONN
  to   = module.uat_ecommerce_tls_cert_service_conn
}

moved {
  from = module.PROD-ECOMMERCE-TLS-CERT-SERVICE-CONN
  to   = module.prod_ecommerce_tls_cert_service_conn
}

moved {
  from = module.DEV-SHARED-TLS-CERT-SERVICE-CONN
  to   = module.dev_shared_tls_cert_service_conn
}

moved {
  from = module.UAT-SHARED-TLS-CERT-SERVICE-CONN
  to   = module.uat_shared_tls_cert_service_conn
}

moved {
  from = module.PROD-SHARED-TLS-CERT-SERVICE-CONN
  to   = module.prod_shared_tls_cert_service_conn
}

# moved {
#   from = module.prod_afm_tls_cert_service_conn
#   to   = module.prod_afm_tls_cert_service_conn
# }

moved {
  from = module.PROD-KIBANA-TLS-CERT-SERVICE-CONN
  to   = module.prod_kibana_tls_cert_service_conn
}

# moved {
#   from = module.PROD-SHARED-TLS-CERT-SERVICE-CONN
#   to   = module.prod_shared_tls_cert_service_conn
# }

moved {
  from = module.DEV-AFM-TLS-CERT-SERVICE-CONN
  to   = module.dev_afm_tls_cert_service_conn
}

moved {
  from = module.UAT-AFM-TLS-CERT-SERVICE-CONN
  to   = module.uat_afm_tls_cert_service_conn
}

moved {
  from = module.PROD-AFM-TLS-CERT-SERVICE-CONN
  to   = module.prod_afm_tls_cert_service_conn
}
